# Chibi хамтрагч — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Апп дотор хосын chibi дүр алхаж, товшиход ичиж, чирж зөөгдөж, товшилт нь хосод push мэдэгдлээр очдог болгох.

**Architecture:** Бүх логик `src/chibi/` доторх 4 шинэ модульд. Төлөвийн машиныг React-аас ангид цэвэр объект (`createBrain`) болгож бичих тул vitest дээр DOM-гүйгээр тестлэнэ. Монолит `tovlorokh-khamtrakh.jsx` дээр зөвхөн интеграцийн цөөн мөр нэмнэ.

**Tech Stack:** React 18, Vite 5, Tailwind 3, Firebase (Firestore + FCM), vitest (энэ төлөвлөгөөгөөр шинээр нэмэгдэнэ).

**Spec:** `docs/superpowers/specs/2026-07-28-chibi-pet-design.md`

## Global Constraints

- Хэрэглэгчид харагдах бүх текст **монгол хэл** дээр. Кодын коммент мөн монголоор — файлын одоогийн хэв маягтай нийцүүлнэ.
- Commit message монголоор, `Co-Authored-By: Claude` мөр **нэмэхгүй**.
- Firestore зам үргэлж `rooms/ankomeow-couple/...`. Кодод `CHAT_ROOM` тогтмолыг ашиглана (утга нь `"ankomeow-couple"`).
- Аккаунтын түлхүүр зөвхөн `"andela"` эсвэл `"neko"`.
- Sprite хуудас: 1024×1024, ил тод дэвсгэр, 3×3 тор, нүд бүр 341×341. Файлын зам `public/chibi/andela.png`, `public/chibi/neko.png`.
- Дэлгэц дээрх chibi-гийн өндөр 72px.
- Push throttle: 60000 мс-д нэг удаа.
- Шинэ npm dependency нэмэхгүй — цорын ганц үл хамаарах зүйл нь Task 1 дэх `vitest` (devDependency).
- Сүлжээний бүх дуудалт `.catch(() => {})` — UI хэзээ ч зогсохгүй.

---

### Task 1: Тест орчин + sprite тодорхойлолт

**Files:**
- Modify: `package.json` (devDependencies, scripts)
- Modify: `vite.config.js:57-61`
- Create: `src/chibi/sprites.js`
- Test: `src/chibi/sprites.test.js`

**Interfaces:**
- Consumes: юу ч үгүй (эхний task).
- Produces:
  - `SPRITE_URL: { andela: string, neko: string }`
  - `CELL: { idle:0, walkA:1, walkB:2, blush:3, happy:4, sit:5, sleep:6, wave:7, dragged:8 }`
  - `cellPosition(index: number) => string` — CSS `background-position` утга
  - `frameFor(state: string, elapsedMs: number) => number` — төлөв ба хугацаанаас нүдний дугаар
  - `SPRITE_HEIGHT = 72`, `SPRITE_WIDTH = 72`

- [ ] **Step 1: vitest суулгах**

```bash
npm install -D vitest@^2
```

- [ ] **Step 2: package.json дээр test script нэмэх**

`package.json`-ы `scripts` хэсэгт нэмнэ:

```json
    "test": "vitest run",
    "test:watch": "vitest"
```

- [ ] **Step 3: vite.config.js дээр test тохиргоо нэмэх**

`defineConfig({...})` доторх сүүлийн мөрийн дараа нэмнэ:

```js
export default defineConfig({
  base: "./",
  plugins: [react(), swPrecachePlugin()],
  server: { port: 5183 },
  test: { environment: "node", include: ["src/**/*.test.js"] },
});
```

- [ ] **Step 4: Унах тест бичих**

`src/chibi/sprites.test.js`:

```js
import { describe, it, expect } from "vitest";
import { CELL, cellPosition, frameFor, SPRITE_URL } from "./sprites.js";

describe("cellPosition", () => {
  it("эхний нүд зүүн дээд буланд байрлана", () => {
    expect(cellPosition(0)).toBe("0% 0%");
  });

  it("хоёрдугаар мөрийн дунд нүд 50% 50% болно", () => {
    expect(cellPosition(4)).toBe("50% 50%");
  });

  it("сүүлийн нүд баруун доод буланд байрлана", () => {
    expect(cellPosition(8)).toBe("100% 100%");
  });
});

describe("frameFor", () => {
  it("алхах үед 320мс тутамд хоёр кадр сольж эргэлдэнэ", () => {
    expect(frameFor("walk", 0)).toBe(CELL.walkA);
    expect(frameFor("walk", 200)).toBe(CELL.walkA);
    expect(frameFor("walk", 400)).toBe(CELL.walkB);
    expect(frameFor("walk", 700)).toBe(CELL.walkA);
  });

  it("алхахаас бусад төлөв тогтмол нүд буцаана", () => {
    expect(frameFor("blush", 0)).toBe(CELL.blush);
    expect(frameFor("blush", 5000)).toBe(CELL.blush);
    expect(frameFor("sleep", 1234)).toBe(CELL.sleep);
    expect(frameFor("dragged", 99)).toBe(CELL.dragged);
    expect(frameFor("land", 0)).toBe(CELL.idle);
  });
});

describe("SPRITE_URL", () => {
  it("хоёр дүрийн зам public/chibi доторх файлыг заана", () => {
    expect(SPRITE_URL.andela).toBe("/chibi/andela.png");
    expect(SPRITE_URL.neko).toBe("/chibi/neko.png");
  });
});
```

- [ ] **Step 5: Тестийг ажиллуулж унасныг батлах**

Run: `npm test`
Expected: FAIL — "Failed to resolve import ./sprites.js"

- [ ] **Step 6: sprites.js бичих**

`src/chibi/sprites.js`:

```js
/* Chibi sprite-ийн цорын ганц эх сурвалж.

   Дүр тус бүр нэг PNG хуудастай: 1024×1024, ил тод дэвсгэр, 3×3 тор.
   CSS дээр background-size: 300% 300% гэж тавихад нүд бүр 0/50/100%-ийн
   алхмаар яг таарна — зураг хуваах скрипт шаардлагагүй.

   Хэрэв AI-гаар үүсгэсэн хуудас жаахан хазайвал энд байгаа GRID эсвэл
   cellPosition-ыг л засна; бусад код өөрчлөгдөхгүй. */

export const SPRITE_URL = {
  andela: "/chibi/andela.png",
  neko: "/chibi/neko.png",
};

export const GRID = 3;

/* Торны нүдний дугаар (зүүнээс баруун, дээрээс доош) */
export const CELL = {
  idle: 0,
  walkA: 1,
  walkB: 2,
  blush: 3,
  happy: 4,
  sit: 5,
  sleep: 6,
  wave: 7,
  dragged: 8,
};

export const SPRITE_WIDTH = 72;
export const SPRITE_HEIGHT = 72;

/* Алхааны хоёр кадр хэдэн мс тутамд солигдох */
export const WALK_FRAME_MS = 320;

export function cellPosition(index) {
  const step = 100 / (GRID - 1);
  const col = index % GRID;
  const row = Math.floor(index / GRID);
  return `${col * step}% ${row * step}%`;
}

/* Төлөв ба тухайн төлөвт орсноос хойшх хугацаанаас харагдах нүдийг олно. */
export function frameFor(state, elapsedMs) {
  if (state === "walk") {
    return Math.floor(elapsedMs / WALK_FRAME_MS) % 2 === 0 ? CELL.walkA : CELL.walkB;
  }
  if (state === "land") return CELL.idle;
  const cell = CELL[state];
  return cell === undefined ? CELL.idle : cell;
}
```

- [ ] **Step 7: Тест ажиллуулж давсныг батлах**

Run: `npm test`
Expected: PASS — 6 тест

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json vite.config.js src/chibi/sprites.js src/chibi/sprites.test.js
git commit -m "test: vitest тохируулж chibi sprite тодорхойлолт нэмэв"
```

---

### Task 2: Төлөвийн машины цөм — алхаа, idle, cute action, унтах

**Files:**
- Create: `src/chibi/brain.js`
- Test: `src/chibi/brain.test.js`

**Interfaces:**
- Consumes: юу ч үгүй (цэвэр модуль, sprites.js-ээс хамаарахгүй).
- Produces:
  - `createBrain({ width, spriteWidth, rand }) => brain`
  - `brain.snapshot() => { state: string, x: number, facing: 1|-1, elapsed: number }`
  - `brain.tick(nowMs) => void`
  - `brain.poke(nowMs) => void`
  - `brain.happy(nowMs) => void`
  - `brain.setWidth(width: number) => void`
  - `DUR: { walkMin, walkMax, idleMin, idleMax, blush, happy, land, cute, sleepAfter }` (мс)
  - `SPEED = 20` (px/сек)

**Тайлбар:** `tick` нь үнэмлэхүй хугацаа (мс) хүлээн авна — React-д `requestAnimationFrame`-ийн timestamp, тестэд гараар өгсөн тоо. Ингэснээр fake timer шаардлагагүй.

- [ ] **Step 1: Унах тест бичих**

`src/chibi/brain.test.js`:

```js
import { describe, it, expect } from "vitest";
import { createBrain, DUR, SPEED } from "./brain.js";

/* Санамсаргүй байдлыг тестэд тогтмол болгоно */
const fixedRand = (v) => () => v;

const makeBrain = (rand = fixedRand(0.5)) =>
  createBrain({ width: 400, spriteWidth: 72, rand });

describe("эхлэлийн төлөв", () => {
  it("алхаж эхэлнэ", () => {
    const b = makeBrain();
    b.tick(0);
    expect(b.snapshot().state).toBe("walk");
  });
});

describe("алхаа", () => {
  it("хугацааны хэрээр баруун тийш SPEED хурдаар шилжинэ", () => {
    const b = makeBrain();
    b.tick(0);
    const startX = b.snapshot().x;
    b.tick(1000);
    expect(b.snapshot().x).toBeCloseTo(startX + SPEED, 5);
  });

  it("баруун ирмэгт хүрэхэд эргэж зүүн тийш явна", () => {
    const b = makeBrain();
    b.tick(0);
    /* хүрээнээс хол гарах хангалттай урт хугацаа */
    b.tick(60000);
    const snap = b.snapshot();
    expect(snap.x).toBeLessThanOrEqual(400 - 72);
    expect(snap.x).toBeGreaterThanOrEqual(0);
    expect(snap.facing).toBe(-1);
  });

  it("алхааны хугацаа дуусахад idle болно", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(DUR.walkMax + 1);
    expect(b.snapshot().state).toBe("idle");
  });
});

describe("cute action", () => {
  it("гурав дахь мөчлөгт санамсаргүй үйлдэл гарна", () => {
    /* rand 0.1 — CUTE_CHANCE (0.34)-аас бага тул cute action үргэлж гарна,
       мөн 0.5-аас бага тул үргэлж "sit" сонгогдоно */
    const b = createBrain({ width: 400, spriteWidth: 72, rand: fixedRand(0.1) });
    let t = 0;
    const states = new Set();
    for (let i = 0; i < 10; i++) {
      t += DUR.walkMax + 1;
      b.tick(t);
      states.add(b.snapshot().state);
      t += DUR.idleMax + 1;
      b.tick(t);
      states.add(b.snapshot().state);
    }
    expect(states.has("sit")).toBe(true);
  });
});

describe("унтах", () => {
  it("хүрэлтгүй sleepAfter хугацаа өнгөрвөл унтана", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(DUR.sleepAfter + 1);
    expect(b.snapshot().state).toBe("sleep");
  });

  it("товшилтын дараа унтах тоолуур дахин эхэлнэ", () => {
    const b = makeBrain();
    b.tick(0);
    b.poke(DUR.sleepAfter - 1000);
    b.tick(DUR.sleepAfter + 1);
    expect(b.snapshot().state).not.toBe("sleep");
  });
});

describe("товшилт", () => {
  it("алхаж байхад товшиход шууд blush болно", () => {
    const b = makeBrain();
    b.tick(0);
    b.poke(1000);
    expect(b.snapshot().state).toBe("blush");
  });

  it("унтаж байхад товшиход сэрж blush болно", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(DUR.sleepAfter + 1);
    expect(b.snapshot().state).toBe("sleep");
    b.poke(DUR.sleepAfter + 2);
    expect(b.snapshot().state).toBe("blush");
  });

  it("blush дууссаны дараа алхаагаа үргэлжлүүлнэ", () => {
    const b = makeBrain();
    b.tick(0);
    b.poke(1000);
    b.tick(1000 + DUR.blush + 1);
    expect(b.snapshot().state).toBe("walk");
  });

  it("blush үед байрлал хөдлөхгүй", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(2000);
    const x = b.snapshot().x;
    b.poke(2000);
    b.tick(2500);
    expect(b.snapshot().x).toBe(x);
  });
});

describe("хос товшсон", () => {
  it("happy төлөвт орж, дараа нь алхаанд буцна", () => {
    const b = makeBrain();
    b.tick(0);
    b.happy(1000);
    expect(b.snapshot().state).toBe("happy");
    b.tick(1000 + DUR.happy + 1);
    expect(b.snapshot().state).toBe("walk");
  });
});

describe("хүрээний өргөн өөрчлөгдөх", () => {
  it("өргөн багасахад chibi хүрээнд эргэж багтана", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(20000);
    b.setWidth(120);
    expect(b.snapshot().x).toBeLessThanOrEqual(120 - 72);
    expect(b.snapshot().x).toBeGreaterThanOrEqual(0);
  });
});
```

- [ ] **Step 2: Тест ажиллуулж унасныг батлах**

Run: `npm test`
Expected: FAIL — "Failed to resolve import ./brain.js"

- [ ] **Step 3: brain.js бичих**

`src/chibi/brain.js`:

```js
/* Chibi-гийн төлөвийн машин.

   React, DOM, Firebase-ийн талаар юу ч мэдэхгүй цэвэр объект — хугацааг
   гаднаас (rAF timestamp эсвэл тестийн тоо) хүлээж авдаг тул fake timer-гүйгээр
   бүрэн тестлэгдэнэ. */

export const SPEED = 20; /* px/сек — удаан, хөөрхөн */

export const DUR = {
  walkMin: 3000,
  walkMax: 6000,
  idleMin: 2000,
  idleMax: 5000,
  cute: 6000,
  blush: 2500,
  happy: 2000,
  land: 350,
  sleepAfter: 60000, /* хүрэлтгүй энэ хугацаа өнгөрвөл унтана */
};

/* Дараалсан хэдэн мөчлөгийн дараа cute action гаргах магадлалыг шалгах босго */
const CUTE_CHANCE = 0.34;

export function createBrain({ width, spriteWidth, rand = Math.random }) {
  let frameWidth = width;
  let state = "walk";
  let x = Math.max(0, (width - spriteWidth) / 2);
  let facing = 1;
  let stateStart = 0;
  let stateDur = 0;
  let now = 0;
  let lastTouch = 0;
  let cycles = 0;

  const maxX = () => Math.max(0, frameWidth - spriteWidth);
  const clampX = () => { x = Math.min(Math.max(x, 0), maxX()); };
  const between = (min, max) => min + rand() * (max - min);

  const enter = (next, at, dur) => {
    state = next;
    stateStart = at;
    stateDur = dur;
  };

  /* Алхаа/idle-ийн мөчлөг дуусахад дараагийн төлөвийг сонгоно */
  const nextInCycle = (at) => {
    if (state === "walk") {
      cycles += 1;
      if (cycles % 3 === 0 && rand() < CUTE_CHANCE) {
        enter(rand() < 0.5 ? "sit" : "wave", at, DUR.cute);
        return;
      }
      enter("idle", at, between(DUR.idleMin, DUR.idleMax));
      return;
    }
    enter("walk", at, between(DUR.walkMin, DUR.walkMax));
  };

  const startWalk = (at) => enter("walk", at, between(DUR.walkMin, DUR.walkMax));

  return {
    snapshot: () => ({ state, x, facing, elapsed: now - stateStart }),

    tick(at) {
      const dt = Math.max(0, at - now);
      now = at;

      if (state === "walk") {
        x += (facing * SPEED * dt) / 1000;
        if (x <= 0) { x = 0; facing = 1; }
        if (x >= maxX()) { x = maxX(); facing = -1; }
      }

      if (state === "dragged") return;

      const elapsed = at - stateStart;

      if (state === "blush" || state === "happy" || state === "land") {
        if (elapsed >= stateDur) startWalk(at);
        return;
      }

      if (state === "sleep") return;

      if (at - lastTouch >= DUR.sleepAfter) {
        enter("sleep", at, Infinity);
        return;
      }

      if (elapsed >= stateDur) nextInCycle(at);
    },

    poke(at) {
      now = at;
      lastTouch = at;
      enter("blush", at, DUR.blush);
    },

    happy(at) {
      now = at;
      lastTouch = at;
      enter("happy", at, DUR.happy);
    },

    setWidth(next) {
      frameWidth = next;
      clampX();
    },
  };
}
```

**Анхаар:** `lastTouch` эхэндээ 0 — эхний 60 секундэд хэрэглэгч хүрэхгүй бол унтана. Энэ нь зориудынх (спекийн "60 сек хүрэлтгүй бол унтана" шаардлага).

- [ ] **Step 4: Тест ажиллуулж давсныг батлах**

Run: `npm test`
Expected: PASS — бүх тест (sprites 6 + brain 12)

- [ ] **Step 5: Commit**

```bash
git add src/chibi/brain.js src/chibi/brain.test.js
git commit -m "feat: chibi төлөвийн машины цөм нэмэв"
```

---

### Task 3: Чирэх ба товшилтыг ялгах

**Files:**
- Modify: `src/chibi/brain.js`
- Modify: `src/chibi/brain.test.js`

**Interfaces:**
- Consumes: Task 2-ын `createBrain`, `DUR`.
- Produces:
  - `brain.pointerDown(nowMs, clientX) => void`
  - `brain.pointerMove(nowMs, clientX) => void`
  - `brain.pointerUp(nowMs) => { tapped: boolean }`
  - `DRAG_THRESHOLD = 6` (px)

- [ ] **Step 1: Унах тест нэмэх**

`src/chibi/brain.test.js`-ийн төгсгөлд нэмнэ (`DRAG_THRESHOLD`-ыг эхний import мөрөнд оруулна: `import { createBrain, DUR, SPEED, DRAG_THRESHOLD } from "./brain.js";`):

```js
describe("чирэх", () => {
  it("6px-ээс бага хөдөлгөөнтэй дарж авбал товшилт болно", () => {
    const b = makeBrain();
    b.tick(0);
    b.pointerDown(100, 200);
    b.pointerMove(150, 203);
    expect(b.pointerUp(200)).toEqual({ tapped: true });
  });

  it("6px-ээс их хөдөлбөл чирэлт болж, товшилт бүртгэгдэхгүй", () => {
    const b = makeBrain();
    b.tick(0);
    b.pointerDown(100, 200);
    b.pointerMove(150, 220);
    expect(b.snapshot().state).toBe("dragged");
    expect(b.pointerUp(200)).toEqual({ tapped: false });
  });

  it("чирэх үед chibi хурууны хөдөлгөөнийг дагана", () => {
    const b = makeBrain();
    b.tick(0);
    const startX = b.snapshot().x;
    b.pointerDown(100, 200);
    b.pointerMove(150, 240);
    expect(b.snapshot().x).toBeCloseTo(startX + 40, 5);
  });

  it("чирэх үед хүрээнээс гарахгүй", () => {
    const b = makeBrain();
    b.tick(0);
    b.pointerDown(100, 200);
    b.pointerMove(150, 5000);
    expect(b.snapshot().x).toBe(400 - 72);
  });

  it("тавихад land төлөвт орж, дараа нь алхаанд буцна", () => {
    const b = makeBrain();
    b.tick(0);
    b.pointerDown(100, 200);
    b.pointerMove(150, 220);
    b.pointerUp(200);
    expect(b.snapshot().state).toBe("land");
    b.tick(200 + DUR.land + 1);
    expect(b.snapshot().state).toBe("walk");
  });

  it("чирэх үед tick байрлалыг хөдөлгөхгүй", () => {
    const b = makeBrain();
    b.tick(0);
    b.pointerDown(100, 200);
    b.pointerMove(150, 220);
    const x = b.snapshot().x;
    b.tick(3000);
    expect(b.snapshot().x).toBe(x);
  });

  it("хоёр дахь хуруу үл тоогдоно", () => {
    const b = makeBrain();
    b.tick(0);
    const startX = b.snapshot().x;
    b.pointerDown(100, 200);
    b.pointerDown(110, 350); /* хоёр дахь хуруу — эхнийхийг дарж бичихгүй */
    b.pointerMove(150, 220);
    /* эхний хурууны 200 → 220 шилжилтээр л тооцоолно, 350-аас биш */
    expect(b.snapshot().x).toBeCloseTo(startX + 20, 5);
    expect(b.pointerUp(200)).toEqual({ tapped: false });
  });

  it("чирэлт унтахыг таслана", () => {
    const b = makeBrain();
    b.tick(0);
    b.tick(DUR.sleepAfter + 1);
    expect(b.snapshot().state).toBe("sleep");
    b.pointerDown(DUR.sleepAfter + 2, 200);
    b.pointerMove(DUR.sleepAfter + 3, 240);
    expect(b.snapshot().state).toBe("dragged");
  });

  it("DRAG_THRESHOLD нь 6px", () => {
    expect(DRAG_THRESHOLD).toBe(6);
  });
});
```

- [ ] **Step 2: Тест ажиллуулж унасныг батлах**

Run: `npm test`
Expected: FAIL — "b.pointerDown is not a function"

- [ ] **Step 3: brain.js дээр чирэлтийн логик нэмэх**

`src/chibi/brain.js`-ийн `SPEED` тодорхойлолтын доор нэмнэ:

```js
/* Дарснаас хойш энэ зайнаас их хөдөлбөл товшилт биш, чирэлт гэж үзнэ */
export const DRAG_THRESHOLD = 6;
```

`createBrain` доторх төлөвийн хувьсагчдын дараа нэмнэ:

```js
  let pointer = null; /* { startClientX, startX, moved } — идэвхтэй нэг л хуруу */
```

`return {...}` объект дотор `happy(at)` методын дараа дараах гурван метод нэмнэ (`enter`, `clampX`, `x`, `state`, `lastTouch` нь closure-т аль хэдийн байгаа):

```js
    pointerDown(at, clientX) {
      if (pointer) return; /* хоёр дахь хуруу — үл тоомсорлоно */
      now = at;
      lastTouch = at;
      pointer = { startClientX: clientX, startX: x, moved: false };
    },

    pointerMove(at, clientX) {
      if (!pointer) return;
      now = at;
      lastTouch = at;
      const dx = clientX - pointer.startClientX;
      if (!pointer.moved && Math.abs(dx) < DRAG_THRESHOLD) return;
      pointer.moved = true;
      if (state !== "dragged") enter("dragged", at, Infinity);
      x = pointer.startX + dx;
      clampX();
    },

    pointerUp(at) {
      if (!pointer) return { tapped: false };
      now = at;
      lastTouch = at;
      const { moved } = pointer;
      pointer = null;
      if (moved) {
        enter("land", at, DUR.land);
        return { tapped: false };
      }
      return { tapped: true };
    },
```

- [ ] **Step 4: Тест ажиллуулж давсныг батлах**

Run: `npm test`
Expected: PASS — бүх тест (sprites 6 + brain 21)

- [ ] **Step 5: Commit**

```bash
git add src/chibi/brain.js src/chibi/brain.test.js
git commit -m "feat: chibi-г чирж зөөх, товшилтоос ялгах логик нэмэв"
```

---

### Task 4: Poke — throttle, Firestore, push

**Files:**
- Create: `src/chibi/poke.js`
- Test: `src/chibi/poke.test.js`

**Interfaces:**
- Consumes: юу ч үгүй (Firestore/push-ыг dependency injection-оор хүлээж авна тул тестэд жинхэнэ Firebase хэрэггүй).
- Produces:
  - `createPokeSender({ writeDoc, sendPush, partnerName, throttleMs }) => { poke(nowMs) }`
  - `POKE_THROTTLE_MS = 60000`
  - `pokeMessage(name: string, count: number) => string`

**Тайлбар:** `writeDoc({ count })` болон `sendPush({ title, body })` нь Promise буцаадаг функцууд. `App`-д эдгээрийг жинхэнэ `setDoc` / `notifyPartner`-оор дүүргэнэ (Task 6).

- [ ] **Step 1: Унах тест бичих**

`src/chibi/poke.test.js`:

```js
import { describe, it, expect, vi } from "vitest";
import { createPokeSender, pokeMessage, POKE_THROTTLE_MS } from "./poke.js";

const makeSender = (over = {}) => {
  const writeDoc = vi.fn(() => Promise.resolve());
  const sendPush = vi.fn(() => Promise.resolve());
  const sender = createPokeSender({ writeDoc, sendPush, partnerName: "Andela", ...over });
  return { sender, writeDoc, sendPush };
};

describe("pokeMessage", () => {
  it("нэг товшилтод ганц тооны текст", () => {
    expect(pokeMessage("Andela", 1)).toBe("Andela чамайг товшлоо 💕");
  });

  it("олон товшилтыг тоогоор нэгтгэнэ", () => {
    expect(pokeMessage("Andela", 5)).toBe("Andela чамайг 5 удаа товшлоо 💕");
  });
});

describe("createPokeSender", () => {
  it("эхний товшилтыг шууд илгээнэ", () => {
    const { sender, writeDoc, sendPush } = makeSender();
    sender.poke(1000);
    expect(writeDoc).toHaveBeenCalledTimes(1);
    expect(writeDoc).toHaveBeenCalledWith({ count: 1 });
    expect(sendPush).toHaveBeenCalledTimes(1);
    expect(sendPush.mock.calls[0][0].body).toBe("Andela чамайг товшлоо 💕");
  });

  it("throttle дотор дахин илгээхгүй", () => {
    const { sender, writeDoc, sendPush } = makeSender();
    sender.poke(1000);
    sender.poke(2000);
    sender.poke(3000);
    expect(writeDoc).toHaveBeenCalledTimes(1);
    expect(sendPush).toHaveBeenCalledTimes(1);
  });

  it("throttle дуусахад хуримтлагдсан тоог нэгтгэж илгээнэ", () => {
    const { sender, sendPush, writeDoc } = makeSender();
    sender.poke(1000);
    sender.poke(2000);
    sender.poke(3000);
    sender.poke(1000 + POKE_THROTTLE_MS);
    expect(writeDoc).toHaveBeenCalledTimes(2);
    expect(writeDoc.mock.calls[1][0]).toEqual({ count: 3 });
    expect(sendPush.mock.calls[1][0].body).toBe("Andela чамайг 3 удаа товшлоо 💕");
  });

  it("throttle дууссаны дараа хуримтлал тэглэгдэнэ", () => {
    const { sender, sendPush } = makeSender();
    sender.poke(1000);
    sender.poke(2000);
    sender.poke(1000 + POKE_THROTTLE_MS);
    sender.poke(1000 + POKE_THROTTLE_MS * 2);
    expect(sendPush.mock.calls[2][0].body).toBe("Andela чамайг товшлоо 💕");
  });

  it("сүлжээ унасан ч алдаа шидэхгүй", () => {
    const { sender } = makeSender({
      writeDoc: () => Promise.reject(new Error("офлайн")),
      sendPush: () => Promise.reject(new Error("офлайн")),
    });
    expect(() => sender.poke(1000)).not.toThrow();
  });

  it("push-ийн гарчиг болон tag тогтмол", () => {
    const { sender, sendPush } = makeSender();
    sender.poke(1000);
    expect(sendPush.mock.calls[0][0].title).toBe("Ankomeow");
    expect(sendPush.mock.calls[0][0].tag).toBe("poke");
  });
});
```

- [ ] **Step 2: Тест ажиллуулж унасныг батлах**

Run: `npm test`
Expected: FAIL — "Failed to resolve import ./poke.js"

- [ ] **Step 3: poke.js бичих**

`src/chibi/poke.js`:

```js
/* Товшилтыг хос руу дамжуулах давхарга.

   Firestore болон push-ыг гаднаас функц хэлбэрээр авдаг тул энэ модуль
   Firebase-ээс бүрэн ангид — тестэд mock л хангалттай.

   Ичих анимаци нь эндээс хамаарахгүй: UI шууд ажиллаад, энэ нь зөвхөн
   сүлжээний талыг throttle-той хариуцна. */

export const POKE_THROTTLE_MS = 60000;

export function pokeMessage(name, count) {
  return count > 1 ? `${name} чамайг ${count} удаа товшлоо 💕` : `${name} чамайг товшлоо 💕`;
}

export function createPokeSender({ writeDoc, sendPush, partnerName, throttleMs = POKE_THROTTLE_MS }) {
  let lastSentAt = -Infinity;
  let pending = 0;

  const flush = (at) => {
    const count = pending;
    pending = 0;
    lastSentAt = at;
    try {
      Promise.resolve(writeDoc({ count })).catch(() => {});
      Promise.resolve(sendPush({
        title: "Ankomeow",
        body: pokeMessage(partnerName, count),
        tag: "poke",
      })).catch(() => {});
    } catch {
      /* дуудлага өөрөө шидсэн ч UI зогсохгүй */
    }
  };

  return {
    poke(at) {
      pending += 1;
      if (at - lastSentAt >= throttleMs) flush(at);
    },
  };
}
```

- [ ] **Step 4: Тест ажиллуулж давсныг батлах**

Run: `npm test`
Expected: PASS — бүх тест (sprites 6 + brain 21 + poke 8)

- [ ] **Step 5: Commit**

```bash
git add src/chibi/poke.js src/chibi/poke.test.js
git commit -m "feat: chibi товшилтын throttle-той илгээгч нэмэв"
```

---

### Task 5: ChibiPet компонент

**Files:**
- Create: `src/chibi/ChibiPet.jsx`

**Interfaces:**
- Consumes: `sprites.js`-ийн `SPRITE_URL, cellPosition, frameFor, SPRITE_WIDTH, SPRITE_HEIGHT`; `brain.js`-ийн `createBrain`.
- Produces: `export default function ChibiPet({ character, enabled, onPoke, happyAt })`
  - `character`: `"andela" | "neko"` — аль дүрийг харуулах (= хосын түлхүүр)
  - `enabled`: `boolean` — false бол юу ч зурахгүй
  - `onPoke`: `() => void` — товшилт бүрт дуудагдана (Task 6-д `pokeSender.poke`-той холбоно)
  - `happyAt`: `number | null` — хос намайг товшсон агшны мс; өөрчлөгдөх бүрт `happy` төлөв ажиллана

Компонент нь **Firebase мэдэхгүй** — зөвхөн props.

- [ ] **Step 1: ChibiPet.jsx бичих**

`src/chibi/ChibiPet.jsx`:

```jsx
import { useEffect, useLayoutEffect, useRef, useState } from "react";
import { createBrain } from "./brain.js";
import { SPRITE_URL, SPRITE_WIDTH, SPRITE_HEIGHT, cellPosition, frameFor } from "./sprites.js";

/* Sprite зураг ачаалагдаагүй үед харагдах энгийн орлуулагч —
   хоосон дөрвөлжин гарахаас сэргийлнэ. */
const PLACEHOLDER =
  "data:image/svg+xml;utf8," +
  encodeURIComponent(
    `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 72 72">
       <circle cx="36" cy="30" r="20" fill="#F2E3D5" stroke="#4A4038" stroke-width="2"/>
       <circle cx="28" cy="30" r="2.6" fill="#4A4038"/>
       <circle cx="44" cy="30" r="2.6" fill="#4A4038"/>
       <circle cx="24" cy="37" r="4" fill="#F0A6A6" opacity="0.75"/>
       <circle cx="48" cy="37" r="4" fill="#F0A6A6" opacity="0.75"/>
       <rect x="28" y="49" width="16" height="16" rx="6" fill="#C9C2B8" stroke="#4A4038" stroke-width="2"/>
     </svg>`
  );

/* Гар нээлттэй эсэхийг --kb-inset-ээс уншина (useKeyboardInset аль хэдийн бичдэг).
   documentElement-ийн style өөрчлөгдөх бүрт л шалгах тул frame бүрт
   getComputedStyle дуудахгүй. */
function useKeyboardOpen() {
  const [open, setOpen] = useState(false);
  useEffect(() => {
    const root = document.documentElement;
    const read = () => {
      const raw = root.style.getPropertyValue("--kb-inset") || "0px";
      setOpen(parseInt(raw, 10) > 0);
    };
    read();
    const obs = new MutationObserver(read);
    obs.observe(root, { attributes: true, attributeFilter: ["style"] });
    return () => obs.disconnect();
  }, []);
  return open;
}

export default function ChibiPet({ character, enabled, onPoke, happyAt }) {
  const layerRef = useRef(null);
  const spriteRef = useRef(null);
  const brainRef = useRef(null);
  const rafRef = useRef(0);
  const [cell, setCell] = useState(0);
  const [facing, setFacing] = useState(1);
  const [state, setState] = useState("walk");
  const [hearts, setHearts] = useState(0); /* зүрхний анимацийг дахин эхлүүлэх түлхүүр */
  const [broken, setBroken] = useState(false);
  const keyboardOpen = useKeyboardOpen();

  const visible = enabled && !keyboardOpen;

  /* Тархийг нэг удаа үүсгэж, хүрээний өргөнд тааруулна */
  useLayoutEffect(() => {
    if (!visible) return;
    const width = layerRef.current?.clientWidth || 360;
    if (!brainRef.current) {
      brainRef.current = createBrain({ width, spriteWidth: SPRITE_WIDTH });
    } else {
      brainRef.current.setWidth(width);
    }
  }, [visible]);

  /* Дэлгэц эргэхэд хүрээнд эргэж багтаана */
  useEffect(() => {
    if (!visible) return;
    const onResize = () => {
      const width = layerRef.current?.clientWidth;
      if (width) brainRef.current?.setWidth(width);
    };
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, [visible]);

  /* Хөдөлгөөний цикл. Байрлалыг transform-оор шууд бичих тул
     React дахин render хийхгүй; зөвхөн төлөв/кадр солигдоход л render болно. */
  useEffect(() => {
    if (!visible) return;
    let running = true;

    const loop = (t) => {
      if (!running) return;
      const brain = brainRef.current;
      if (brain) {
        brain.tick(t);
        const s = brain.snapshot();
        if (spriteRef.current) {
          spriteRef.current.style.transform = `translate3d(${s.x}px, 0, 0)`;
        }
        const nextCell = frameFor(s.state, s.elapsed);
        setCell((c) => (c === nextCell ? c : nextCell));
        setFacing((f) => (f === s.facing ? f : s.facing));
        setState((st) => (st === s.state ? st : s.state));
      }
      rafRef.current = requestAnimationFrame(loop);
    };

    const start = () => {
      if (!rafRef.current) rafRef.current = requestAnimationFrame(loop);
    };
    const stop = () => {
      cancelAnimationFrame(rafRef.current);
      rafRef.current = 0;
    };

    const onVisibility = () => (document.hidden ? stop() : start());
    document.addEventListener("visibilitychange", onVisibility);
    if (!document.hidden) start();

    return () => {
      running = false;
      stop();
      document.removeEventListener("visibilitychange", onVisibility);
    };
  }, [visible]);

  /* Хос намайг товшсон — happy үсрэлт */
  useEffect(() => {
    if (!happyAt || !brainRef.current) return;
    brainRef.current.happy(performance.now());
    setHearts((h) => h + 1);
  }, [happyAt]);

  const onPointerDown = (e) => {
    e.currentTarget.setPointerCapture?.(e.pointerId);
    brainRef.current?.pointerDown(performance.now(), e.clientX);
  };

  const onPointerMove = (e) => {
    brainRef.current?.pointerMove(performance.now(), e.clientX);
  };

  const onPointerUp = () => {
    const res = brainRef.current?.pointerUp(performance.now());
    if (!res?.tapped) return;
    brainRef.current?.poke(performance.now());
    setHearts((h) => h + 1);
    navigator.vibrate?.(12);
    onPoke?.();
  };

  if (!visible) return null;

  const url = broken ? PLACEHOLDER : SPRITE_URL[character];
  const sheet = broken
    ? { backgroundImage: `url(${url})`, backgroundSize: "100% 100%", backgroundPosition: "0% 0%" }
    : { backgroundImage: `url(${url})`, backgroundSize: "300% 300%", backgroundPosition: cellPosition(cell) };

  return (
    <div ref={layerRef} className="absolute inset-0 z-[25] pointer-events-none overflow-hidden">
      {/* зураг ачаалагдахгүй бол орлуулагч руу шилжинэ */}
      <img src={SPRITE_URL[character]} alt="" className="hidden" onError={() => setBroken(true)} />

      <div
        ref={spriteRef}
        onPointerDown={onPointerDown}
        onPointerMove={onPointerMove}
        onPointerUp={onPointerUp}
        onPointerCancel={onPointerUp}
        className="absolute pointer-events-auto touch-none select-none"
        style={{
          width: SPRITE_WIDTH,
          height: SPRITE_HEIGHT,
          bottom: "calc(var(--chibi-baseline, 84px))",
          left: 0,
          transform: "translate3d(0,0,0)",
          backgroundRepeat: "no-repeat",
          ...sheet,
          scale: facing === -1 ? "-1 1" : "1 1",
          transition: state === "land" ? "translate 200ms cubic-bezier(.2,.8,.3,1)" : "none",
        }}
      >
        {state === "sleep" && (
          <span className="absolute -top-2 right-0 text-[11px] font-extrabold chibi-float" style={{ color: "#8A8079" }}>
            zZ
          </span>
        )}
      </div>

      {(state === "blush" || state === "happy") && (
        <div key={hearts} className="absolute pointer-events-none" style={{ bottom: "calc(var(--chibi-baseline, 84px) + 48px)", left: 0 }}>
          <span className="chibi-heart" style={{ animationDelay: "0ms" }}>💗</span>
          <span className="chibi-heart" style={{ animationDelay: "160ms" }}>💗</span>
          <span className="chibi-heart" style={{ animationDelay: "320ms" }}>💗</span>
        </div>
      )}

      <style>{`
        .chibi-heart{position:absolute;font-size:13px;opacity:0;animation:chibi-heart-up 1.4s ease-out forwards}
        .chibi-heart:nth-child(2){left:14px}
        .chibi-heart:nth-child(3){left:-12px}
        @keyframes chibi-heart-up{
          0%{opacity:0;transform:translateY(0) scale(.6)}
          25%{opacity:1}
          100%{opacity:0;transform:translateY(-34px) scale(1)}
        }
        .chibi-float{animation:chibi-float 1.8s ease-in-out infinite}
        @keyframes chibi-float{0%,100%{transform:translateY(0);opacity:.5}50%{transform:translateY(-4px);opacity:1}}
        @media (prefers-reduced-motion: reduce){
          .chibi-heart,.chibi-float{animation:none}
        }
      `}</style>
    </div>
  );
}
```

**Анхаар:** зүрхнүүд chibi-гийн байрлалыг дагахгүй, зүүн ирмэгээс зурагдана — Step 2-т үүнийг chibi-тэй хамт хөдөлгөнө.

- [ ] **Step 2: Зүрхийг chibi-гийн байрлалд наах**

Дээрх зүрхний `div`-д `ref` нэмж, rAF цикл дотор sprite-тай хамт байрлалыг бичнэ. `ChibiPet` дотор:

```jsx
  const heartsRef = useRef(null);
```

rAF циклийн `spriteRef.current.style.transform = ...` мөрийн доор нэмнэ:

```jsx
        if (heartsRef.current) {
          heartsRef.current.style.transform = `translate3d(${s.x + SPRITE_WIDTH / 2}px, 0, 0)`;
        }
```

Зүрхний `div`-д `ref={heartsRef}` нэмнэ.

- [ ] **Step 3: Baseline CSS хувьсагч нэмэх**

`src/index.css`-ийн `:root` блокт нэмнэ (байхгүй бол файлын эхэнд `:root{}` үүсгэнэ):

```css
:root{
  /* chibi таб барын дээгүүр алхана */
  --chibi-baseline: 84px;
}
```

- [ ] **Step 4: Build ажиллаж байгааг шалгах**

Run: `npm run build`
Expected: амжилттай, алдаагүй

- [ ] **Step 5: Тест дахин ажиллуулах (регресс шалгах)**

Run: `npm test`
Expected: PASS — өмнөх бүх тест хэвээр

- [ ] **Step 6: Commit**

```bash
git add src/chibi/ChibiPet.jsx src/index.css
git commit -m "feat: chibi-гийн харагдах давхарга болон анимацууд нэмэв"
```

---

### Task 6: App интеграци + Firestore дүрэм

**Files:**
- Modify: `tovlorokh-khamtrakh.jsx:1-11` (import)
- Modify: `tovlorokh-khamtrakh.jsx` — `App` доторх state/effect хэсэг (`peeks` сонсогчийн дэргэд, ~line 1995)
- Modify: `tovlorokh-khamtrakh.jsx:2206` орчим (`app-frame` доторх render)
- Modify: `firestore.rules`

**Interfaces:**
- Consumes: `ChibiPet` (Task 5), `createPokeSender` (Task 4).
- Produces: `App` дотор `chibiEnabled` state — Task 7 үүнийг ProfileScreen рүү дамжуулна.

- [ ] **Step 1: Import нэмэх**

`tovlorokh-khamtrakh.jsx`-ийн 11-р мөрийн дараа:

```js
import ChibiPet from "./src/chibi/ChibiPet.jsx";
import { createPokeSender } from "./src/chibi/poke.js";
```

- [ ] **Step 2: App дотор chibi-гийн state нэмэх**

`const [peekToast, setPeekToast] = useState(null);` мөрийн доор:

```js
  const [chibiEnabled, setChibiEnabled] = useState(() => localStorage.getItem("ankomeow-chibi-off") !== "1");
  const [chibiHappyAt, setChibiHappyAt] = useState(null);
```

- [ ] **Step 3: Poke илгээгч үүсгэх**

`peeks` сонсох effect-ийн доор нэмнэ:

```js
  /* Chibi товшилтыг хос руу дамжуулах илгээгч — хосын түлхүүр солигдвол шинэчлэгдэнэ */
  const pokeSender = useMemo(() => {
    if (!partnerKey) return null;
    return createPokeSender({
      partnerName: ACCOUNTS[partnerKey]?.name || "Хамтрагч",
      writeDoc: ({ count }) =>
        setDoc(doc(db, "rooms", CHAT_ROOM, "pokes", partnerKey), {
          from: accountKey, count, at: serverTimestamp(),
        }),
      sendPush: ({ title, body, tag }) =>
        notifyPartner(auth, { to: partnerKey, title, body, tag, tab: "home" }),
    });
  }, [accountKey, partnerKey]);
```

- [ ] **Step 4: Хосын товшилтыг сонсох**

`peeks` сонсох effect-ийн загварыг дуурайж нэмнэ:

```js
  /* Хос миний chibi-г товшлоо — дэлгэц дээрх chibi баярлана */
  useEffect(() => {
    if (!accountKey) return;
    const unsub = onSnapshot(doc(db, "rooms", CHAT_ROOM, "pokes", accountKey), (snap) => {
      const at = snap.data()?.at;
      if (!at) return;
      const ms = at.toMillis();
      const lastSeen = Number(localStorage.getItem("ankomeow-last-poke") || 0);
      if (ms > lastSeen) {
        localStorage.setItem("ankomeow-last-poke", String(ms));
        setChibiHappyAt(ms);
      }
    }, () => {});
    return unsub;
  }, [accountKey]);
```

- [ ] **Step 5: ChibiPet-ийг render хийх**

`app-frame` доторх mac-dots блокийн **дараа** нэмнэ (`{peekToast && (` мөрийн өмнө):

```jsx
        {/* Хосын chibi — зөвхөн нэвтэрсэн, splash дууссан үед */}
        {booted && user && partnerKey && (
          <ChibiPet
            character={partnerKey}
            enabled={chibiEnabled}
            happyAt={chibiHappyAt}
            onPoke={() => pokeSender?.poke(Date.now())}
          />
        )}
```

- [ ] **Step 6: Firestore дүрэм нэмэх**

`firestore.rules`-ийн `peeks` блокийн дараа нэмнэ:

```
    /* Chibi товшилт — хос бичиж, эзэн нь уншина (peeks-тэй ижил загвар) */
    match /rooms/ankomeow-couple/pokes/{key} {
      allow read: if isCouple() && key == myKey();
      allow write: if isCouple();
    }
```

- [ ] **Step 7: Build ба тест**

Run: `npm run build && npm test`
Expected: build амжилттай, бүх тест PASS

- [ ] **Step 8: Гараар шалгах**

Run: `npm run dev`
Шалгах зүйлс:
1. Нэвтрэхийн өмнө chibi харагдахгүй; нэвтэрч splash дууссаны дараа гарч ирнэ.
2. Chibi таб барын дээгүүр алхана, ирмэгт хүрч эргэнэ.
3. Товшиход ичих кадр + зүрх гарна.
4. Чирж зөөгдөнө, тавихад буулт хийж алхаагаа үргэлжлүүлнэ.
5. Чат руу орж гар нээхэд chibi алга болно, гар хаагдахад буцаж ирнэ.
6. Бусад дэлгэцийн товчлуурууд, scroll хэвийн ажиллана.

- [ ] **Step 9: Firestore дүрэм deploy хийх**

Run: `npx firebase deploy --only firestore:rules`
Expected: "Deploy complete"

(Хэрэв firebase CLI нэвтрээгүй бол алгасаад хэрэглэгчид мэдэгдэнэ — `pokes` дүрэм гарахгүй бол товшилт зөвхөн локал ажиллана.)

- [ ] **Step 10: Commit**

```bash
git add tovlorokh-khamtrakh.jsx firestore.rules
git commit -m "feat: chibi хамтрагчийг апп дотор холбож, товшилтыг хосод дамжуулав"
```

---

### Task 7: Профайл дээрх унтраалга

**Files:**
- Modify: `tovlorokh-khamtrakh.jsx` — `ProfileScreen` (~line 1443 тодорхойлолт, ~line 1530 render төгсгөл)
- Modify: `tovlorokh-khamtrakh.jsx` — `App` доторх `ProfileScreen` дуудалт (~line 2245)

**Interfaces:**
- Consumes: Task 6-ийн `chibiEnabled`, `setChibiEnabled`.
- Produces: юу ч үгүй (сүүлийн хэрэглэгч).

- [ ] **Step 1: ProfileScreen-д props нэмэх**

Тодорхойлолтыг өөрчилнө:

```jsx
function ProfileScreen({ ml, goal, items, gifCount, screenApps, appMin, avatar, setAvatar, profileName, chibiEnabled, setChibiEnabled, onBack }) {
```

- [ ] **Step 2: Унтраалгын карт нэмэх**

`ProfileScreen`-ийн `return`-ийн төгсгөлд, "Өнөөдрийн явц" grid-ийн **дараа**, хаах `</div>`-ийн өмнө нэмнэ:

```jsx
      <div className="text-[13px] font-extrabold mt-4 mb-2.5" style={{ color: C.ink }}>Тохиргоо</div>
      <Card tint="#F8F4FC">
        <div className="flex items-center justify-between gap-3">
          <div className="min-w-0">
            <div className="text-[13px] font-extrabold mb-0.5" style={{ color: C.ink }}>Chibi хамтрагч</div>
            <div className="text-[11.5px] font-bold leading-snug" style={{ color: C.inkSoft }}>
              Дэлгэц дээр алхаж яваа жижиг дүр
            </div>
          </div>
          <button
            onClick={() => setChibiEnabled((v) => !v)}
            aria-label="Chibi хамтрагчийг асаах/унтраах"
            aria-pressed={chibiEnabled}
            className="w-12 h-7 rounded-full shrink-0 relative active:scale-95"
            style={{
              background: chibiEnabled ? C.lilacDeep : C.line2,
              transition: "background 200ms ease, transform 150ms ease",
            }}>
            <span className="absolute top-1 w-5 h-5 rounded-full bg-white shadow"
              style={{ left: chibiEnabled ? 26 : 4, transition: "left 200ms cubic-bezier(.2,.8,.3,1)" }} />
          </button>
        </div>
      </Card>
```

- [ ] **Step 3: App дээр props дамжуулах**

`{tab === "profile" && <ProfileScreen ... />}` мөрийг өөрчилнө:

```jsx
                {tab === "profile" && <ProfileScreen {...{ ml, goal, items, screenApps, appMin, avatar, setAvatar, profileName, chibiEnabled, setChibiEnabled }} gifCount={frames.length} onBack={() => go("home")} />}
```

- [ ] **Step 4: Сонголтыг localStorage-д хадгалах**

`App` дотор `chibiEnabled` state-ийн доор нэмнэ:

```js
  useEffect(() => {
    if (chibiEnabled) localStorage.removeItem("ankomeow-chibi-off");
    else localStorage.setItem("ankomeow-chibi-off", "1");
  }, [chibiEnabled]);
```

- [ ] **Step 5: Build ба гараар шалгах**

Run: `npm run build`
Expected: амжилттай

Run: `npm run dev` — Профайл → "Chibi хамтрагч" унтраахад chibi алга болно; аппыг refresh хийсэн ч унтраалттай хэвээр; асаахад буцаж ирнэ.

- [ ] **Step 6: Commit**

```bash
git add tovlorokh-khamtrakh.jsx
git commit -m "feat: профайл дээр chibi хамтрагчийн унтраалга нэмэв"
```

---

### Task 8: Жинхэнэ sprite зургийг суулгах

**Files:**
- Create: `public/chibi/andela.png`
- Create: `public/chibi/neko.png`

**Interfaces:**
- Consumes: `sprites.js`-ийн `SPRITE_URL` (зам аль хэдийн зөв заасан тул код өөрчлөх шаардлагагүй).
- Produces: юу ч үгүй.

**Тайлбар:** Зургийг хэрэглэгч GPT-ээр үүсгэнэ. Prompt-ууд спекийн "GPT-д өгөх зургийн prompt" хэсэгт бэлэн байгаа: `docs/superpowers/specs/2026-07-28-chibi-pet-design.md`.

- [ ] **Step 1: Зургийг байрлуулах**

```bash
mkdir -p public/chibi
# andela.png, neko.png -г public/chibi/ дотор хуулна
```

Шаардлага: 1024×1024, ил тод дэвсгэр (PNG), 3×3 тор.

- [ ] **Step 2: Хэмжээ болон ил тод байдлыг шалгах**

```bash
file public/chibi/andela.png public/chibi/neko.png
```

Expected: хоёулаа `PNG image data, 1024 x 1024, 8-bit/color RGBA` — `RGBA` биш бол ил тод дэвсгэргүй, дахин үүсгэнэ.

- [ ] **Step 3: Гараар шалгах**

Run: `npm run dev`
Шалгах: chibi placeholder биш жинхэнэ дүр болсон; алхах үед хоёр кадр солигдоно; товшиход ичсэн кадр гарна; зүүн тийш явахад дүр эргэж харагдана.

- [ ] **Step 4: Тор хазайсан бол тохируулах**

Хэрэв нүд бүр яг таарахгүй (дүрийн хэсэг тасарч, эсвэл хөрш нүднээс өнгө орж ирвэл) `src/chibi/sprites.js` доторх `cellPosition`-д тохируулга нэмнэ. Жишээ нь торны захад 8px хоосон зай үлдсэн бол:

```js
export function cellPosition(index) {
  const step = 100 / (GRID - 1);
  const col = index % GRID;
  const row = Math.floor(index / GRID);
  return `${col * step}% ${row * step}%`;
}
```

-ыг дараах байдлаар солино (`INSET`-ийг зургийн бодит хазайлтаар тааруулна):

```js
/* AI-аар үүсгэсэн хуудсанд нүд бүр яг 1/3 биш байвал энд тохируулна */
const INSET = 0; /* нүд бүрийн эргэн тойрны хоосон зай, % */

export function cellPosition(index) {
  const step = 100 / (GRID - 1);
  const col = index % GRID;
  const row = Math.floor(index / GRID);
  const adj = (v) => Math.min(100, Math.max(0, v + (v - 50) * (INSET / 100)));
  return `${adj(col * step)}% ${adj(row * step)}%`;
}
```

Тохируулсан бол `npm test` ажиллуулж `cellPosition`-ы тестийг шинэ утгад тааруулан шинэчилнэ.

- [ ] **Step 5: Commit**

```bash
git add public/chibi/andela.png public/chibi/neko.png
git commit -m "feat: chibi дүрсийн sprite зургууд нэмэв"
```

---

## Дууссаны дараах шалгалт

- [ ] `npm test` — бүх тест PASS
- [ ] `npm run build` — алдаагүй
- [ ] Хоёр хөтөч дээр Andela болон Neko-гоор нэвтэрч, нэг талаас chibi-г товшиход нөгөө талын chibi баярлаж байгааг харах
- [ ] Аппыг хааж push мэдэгдэл ирж байгааг шалгах (iOS дээр зөвхөн Home Screen-д суулгасан үед)
- [ ] Дараалан 10 удаа товшиход зөвхөн 1 push ирсэн эсэхийг шалгах
